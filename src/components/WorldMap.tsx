'use client';

/**
 * WorldMap component is disabled because SEOLocation data
 * represents fictional locations in Dimension GG4.261T,
 * not real-world coordinates.
 * 
 * This component can be re-enabled if location coordinates are added to the data.
 */
export default function WorldMap() {
    return null;
}
